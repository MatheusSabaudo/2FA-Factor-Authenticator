# 2FA-Factor-Authenticator

A simple Two Factor Authenticator using the "pyotp" to generate OTP codes to access services around the internet safely.

Requirements:
pip install pyotp
pip install qrcode
